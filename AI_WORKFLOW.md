# AI workflow

This is a downstream fork. Keep this rollout to the policy layer. Any future applet or plugin work
must name the maintained paths, use a focused upstream-native check, and leave installation,
runtime changes, and publishing outside the task unless separately approved.
